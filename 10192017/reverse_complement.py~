#!/usr/bin/env Python3


fastQ = open("Python_05.fasta", "r")


dictionary = {}

fastread = fastQ.read()
faststrip = fastread.split()

#print (faststrip)

for str in faststrip:

    print ("this is" line)
    #geneID = genes[0]
    #geneseq = genes[1]
    #print ("gene ID", geneID, "geneseq", geneseq)
#i, seq  = stripline.split()
    


    #print ("this is .split", seq) 
    #id,seq =line.split()
    #dictionary[id] =seq

#print (dictionary)



#open
# print reverse complement
# print output in fasta format including sequence name and note that it's the reverse complemt
